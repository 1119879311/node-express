{
  "name": "express",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development nodemon src/index.js --ext js,mjs,json,env  --watch src --watch config",
    "build": "cross-env NODE_ENV=production&&tsc && nodemon  src/index.js --ext js,mjs,json,env --watch src --watch config"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "art-template": "^4.13.2",
    "connect-history-api-fallback": "^2.0.0",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-art-template": "^1.0.1",
    "nodemon": "^2.0.20"
  }
}
